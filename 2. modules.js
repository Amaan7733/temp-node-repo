// CommonJS, every file in node is a module (by default)

// Modules => Encapsulted code (only gives minimun)

const sayHi = (name) => {
    console.log(`Hello there ${name}`);
}

sayHi("amaan");
sayHi("sumbul");
sayHi(rashid);
sayHi(iftekhar);
